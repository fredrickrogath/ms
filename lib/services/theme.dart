import 'package:flutter/cupertino.dart';
// import 'package:shared_preferences/shared_preferences.dart';

class DarkMode extends ChangeNotifier {
  // late bool isDark = false;

  // bool get getCurrentTheme => isDark;

  // checkTheme() async {
  //   SharedPreferences prefs = await SharedPreferences.getInstance();
  //   return prefs.getBool('isDark');
  // }

  // void getTheme() async {
  //   SharedPreferences localStorage = await SharedPreferences.getInstance();
  //   localStorage.getBool('isDark') != null
  //       ? isDark = localStorage.getBool('isDark')!
  //       : isDark = false;
        
  //   notifyListeners();
  // }

  // void saveTheme(value) async {
  //   SharedPreferences localStorage = await SharedPreferences.getInstance();
  //   localStorage.setBool('isDark', value);

  //   getTheme();

  //   notifyListeners();
  // }
}
